import React from "react";

const Login = () => {
  return (
    <form>
      <input type="text" />
      <input type="password" />
      <button>Submit</button>
    </form>
  );
};

export default Login;